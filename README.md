# Call Center Platform

Monorepo containing:
- `frontend/` — Angular agent portal → deployed on **Vercel**
- `backend/` — .NET 10 API + SignalR hub → deployed on **Railway**

See `DEPLOY.md` for the step-by-step setup for both platforms.
